# x lang

这是一个低代码流程图编程语言，用于用户自定义脚本，并起到沙箱功能。

使用函数式编程，对于流程控制，只有条件判断，循环使用递归实现。

在结合编辑器使用时，可以自动为节点起名，所以整个编程过程只要为函数起名。

支持类型，支持泛型。

数据存储为 JSON，可以使用 js 调用运行。

java(script^2)

可以把代码渲染为魔法阵。

![代码魔法阵](https://youke1.picui.cn/s1/2025/11/11/69133ab5245cb.png)
